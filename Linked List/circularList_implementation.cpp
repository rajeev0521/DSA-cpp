#include <bits/stdc++.h>
using namespace std;

class List{
    public:
    int data;
    List* next;

    public:
    // constructor with an initializer‑list
    List(int val, List* nextNode = nullptr) 
        : data(val), next(nextNode) {}
};


int main(){

    return 0;
}